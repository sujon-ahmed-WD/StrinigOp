#include<stdio.h>
#include<string.h>
int main()
{
    char a[20]="jj";
    char b[20]="Love for Phitron";
    // int length=strlen(b);
    // printf("%s %s\n",a,b);

    // strcpy(a,b); // b ar value golo a hoba .... 
    strcpy(a , "Love From in Codinge");
    printf("%s",a);

     
    
    // for(int i=0; i<length; i++){
    //     a[i]=b[i];
    // }
    // a[length]='\0';

    // printf("%s a =>%s b\n",a,b);
    return 0;
}